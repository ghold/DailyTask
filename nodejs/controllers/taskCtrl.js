var _ = require("lodash");
var taskModel = require('../models/taskDao').TaskModel;
var errors = require('../exception/exceptionCon').errors;
var warns = require('../exception/exceptionCon').warns;;

/**
 *
 * 描述：任务实体处理
 *
 * HISTORY
 * ****************************************************************************
 *  ID   DATE           			         PERSON          REASON
 *  1    2015年7月18日 下午8:53:38      Ghold          Create
 * ****************************************************************************
 */

var taskCtrl = {
    autoGenTask : function(req, res, )
};

module.exports.__proto__ = taskCtrl;
